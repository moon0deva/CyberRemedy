# ─── CyberRemedy Requirements────────────────────────────────────────────
# Python compatibility: 3.9 – 3.13+
#
# Quick-start by Python version:
#   Python 3.9 – 3.12:  pip install -r requirements.txt
#   Python 3.13+:       pip install -r requirements.txt  (TF skipped automatically,
#                       PyTorch fills in; torch is listed with no-version-pin below)
#
# The ML trainer auto-detects the best backend at runtime. No manual config needed.

# ── Core API ───────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic>=2.0

# ── Packet capture & flow analysis ────────────────────────────────────────────
scapy>=2.5.0
pyshark>=0.6

# ── Data processing ───────────────────────────────────────────────────────────
# numpy: 1.26.x supports Python 3.9–3.12; 2.x supports 3.9–3.13+
# pip resolves the right version automatically.
numpy>=1.26.4
pandas>=2.2.2

# ── ML core (always required) ─────────────────────────────────────────────────
scikit-learn>=1.4.2
joblib>=1.4.2
scipy>=1.13.0

# ── ML backend — TensorFlow (Python 3.9–3.12) ─────────────────────────────────
# TensorFlow does not publish Python 3.13 wheels yet (as of 2026-03).
# The trainer.py auto-skips TF and falls back to PyTorch on Python 3.13+.
# Install manually when on Python ≤ 3.12:
#   pip install tensorflow
# Or let the line below handle it (pip will skip it on 3.13+ if no wheel exists):
tensorflow>=2.16.0; python_version < "3.13"

# ── ML backend — PyTorch (Python 3.9–3.13+, universal fallback) ───────────────
# PyTorch is the only backend available on Python 3.13+.
# It also works on 3.9–3.12 if you prefer it over TensorFlow.
# Install CPU-only (smaller download):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Install with GPU support:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.1.0; python_version >= "3.13"
# Uncomment to install torch on older Python too (replaces TF):
# torch>=2.1.0

# ── System utilities ───────────────────────────────────────────────────────────
psutil>=5.9.8
python-dotenv>=1.0.1
pyyaml>=6.0.1

# ── Reporting & templates ──────────────────────────────────────────────────────
jinja2>=3.1.4
aiofiles>=23.2.1

# ── Async support ──────────────────────────────────────────────────────────────
asyncio-throttle>=1.0.2

# ── v3.0 New Module Dependencies ───────────────────────────────────────────────

# YARA rule scanning (optional — pure-python fallback built-in)
yara-python>=4.5.1

# Sigma rule support
sigma-cli>=0.10.0
ruamel.yaml>=0.18

# Threat intelligence feeds
requests>=2.32.3
stix2>=3.0.0

# RBAC / Authentication
bcrypt>=4.1.3
PyJWT>=2.8.0

# Case management persistence
tinydb>=4.8.0

# Compliance & vulnerability
cvss>=2.6
packaging

# Data lake / storage
pyarrow>=16.0
zstd

# GeoIP (optional)
maxminddb

# Network interface detection (optional)
netifaces

# ── TLS/HTTPS Interception ─────────────────────────────────────────────────────
mitmproxy>=10.0.0
cryptography>=41.0.0
pyOpenSSL>=23.0.0

# ── System packages (apt/brew, NOT pip) ────────────────────────────────────────
# sudo apt install arp-scan nmap iptables aircrack-ng
