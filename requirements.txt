# ─── CyberRemedy Requirements v3.0 ────────────────────────────────────────────────

# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
pydantic>=2.0

# Packet capture & flow analysis
scapy==2.5.0
pyshark==0.6

# Data processing
pandas==2.2.2
numpy==1.26.4

# ML models
scikit-learn==1.4.2
joblib==1.4.2
scipy==1.13.0

# System utilities
psutil==5.9.8
python-dotenv==1.0.1
pyyaml==6.0.1

# Reporting & templates
jinja2==3.1.4
aiofiles==23.2.1

# Async support
asyncio-throttle==1.0.2

# ─── v3.0 New Module Dependencies ─────────────────────────────────────────────

# YARA rule scanning (optional — pure-python fallback built-in)
yara-python==4.5.1

# Sigma rule support
sigma-cli>=0.10.0        # sigma rule parsing
ruamel.yaml>=0.18        # YAML round-trip for Sigma

# Threat intelligence feeds
requests==2.32.3
stix2>=3.0.0             # STIX/TAXII feed support (optional)

# RBAC / Authentication
bcrypt==4.1.3
PyJWT==2.8.0

# Case management persistence
tinydb==4.8.0            # lightweight local DB (no external server)

# Compliance & vulnerability
cvss==2.6               # CVSS score calculation
packaging>=24.0         # version comparison for vuln matching

# Data lake / storage
pyarrow>=16.0           # Parquet format for efficient data lake storage
zstd>=1.5               # compression for archived data

# Honeypot services (optional — simulation fallback built-in)
# opencanary is optional; the module has a pure-python socket honeypot

# v4.0 (all optional — app runs without them)
maxminddb-reader>=1.5.0   # offline MaxMind GeoIP (optional - free CSV used by default)
netifaces>=0.11.0         # better network interface detection
# System packages (install with apt/brew, not pip):
# sudo apt install arp-scan nmap
